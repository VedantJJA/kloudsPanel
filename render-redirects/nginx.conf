server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # The redirector already resolves rewrites/redirects before a request
    # ever reaches here, so this just serves whatever exact path it's
    # asked for.
    location / {
        try_files $uri =404;
    }
}
